# dependencies
node_modules

# build output
dist

# env / secrets (API keys live here — never commit)
.env
.env.local
.env.*.local

# local database (accounts + favorites)
server/*.db
server/*.db-*

# misc
.DS_Store
*.log
.vite
