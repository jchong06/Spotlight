# Fly.io config for Spotlight.
# Rename `app` to something unique (fly launch will offer to do this), then:
#   fly secrets set TM_KEY=... SEATGEEK_CLIENT_ID=...
#   fly deploy
# The Supabase URL + anon key are public, so pass them as build args
# (they get baked into the static bundle by Vite):
#   fly deploy --build-arg VITE_SUPABASE_URL=... --build-arg VITE_SUPABASE_ANON_KEY=...
# Accounts + favorites live in Supabase now, so the app no longer writes to a
# volume. The existing `spotlight_data` volume is left declared (and unused)
# only because this machine was created with it; it can be removed later by
# destroying the volume. The app itself stores nothing on disk.
app = "spotlight-edm-30822"
primary_region = "ewr"

[build]

[env]
  PORT = "8080"

[mounts]
  source = "spotlight_data"
  destination = "/data"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

  [[http_service.checks]]
    grace_period = "10s"
    interval = "30s"
    method = "GET"
    path = "/"
    timeout = "5s"
